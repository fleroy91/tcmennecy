<%= render 'refinery/admin/search_header', :url => refinery.res_sportifs_admin_res_sportifs_path %>
<div class='pagination_container'>
  <% if @res_sportifs.any? %>
    <%= render 'res_sportifs' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
