<%= render 'refinery/admin/search_header', :url => refinery.competitions_admin_competitions_path %>
<div class='pagination_container'>
  <% if @competitions.any? %>
    <%= render 'competitions' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
